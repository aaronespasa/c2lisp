Para compilar con bison:
>>> bison -d trad2.y

Para compilar el código de C que te generó bison:
>>> gcc -o trad2 trad2.tab.c

Para ejecutar el archivo compilado con C:
>>> ./trad2

bison -d trad2.y && gcc -o trad2 trad2.tab.c && ./trad2